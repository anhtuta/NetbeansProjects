Note: this server package can be a separate project, which means you can create 2 projects:
- one includes these packages: controller, images, model, view. This project names: chat_client
- one has only this package: server. This project names: chat_server
project chat_server only places in server computer, and all of users just need project chat_client to run
Note: server computer need MySQL.

